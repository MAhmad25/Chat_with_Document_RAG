# For local dev, leave empty — the Vite dev server proxy (see vite.config.ts)
# forwards /api requests to http://127.0.0.1:8000.
#
# For production (Vercel), set this to your deployed backend's URL, e.g.:
# VITE_API_BASE_URL=https://your-app.onrender.com
VITE_API_BASE_URL=
